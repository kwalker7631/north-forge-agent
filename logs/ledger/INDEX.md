# North-Forge Ledger Index

Master pointer for the project ledger. Newest first. Rules: [`README.md`](./README.md).

- **Changelog:** [`CHANGELOG.md`](./CHANGELOG.md) — every tracked-file / config change.
- **Error register:** [`errors/ERROR-LOG.md`](./errors/ERROR-LOG.md) — faults (breaks, regressions, leaks).
- **Decision register:** [`decisions/DECISION-LOG.md`](./decisions/DECISION-LOG.md) — open judgment calls.
- **Templates:** [`templates/`](./templates/) — copy when adding an audit / change / error / decision.

## Current coordinates

| Coordinate | Value | As of |
| --- | --- | --- |
| Upstream base | `hermes@8d79c2ff57` — **228 behind `upstream/main`** as of `RUN-2026-09-11-002` (base sha unchanged; upstream simply advanced further during this run's `git fetch upstream`, done while investigating ERR-2026-09-11-001). Prior: **217 behind `upstream/main`** as of `RUN-2026-09-11-001` (`git fetch upstream`, `upstream` remote re-added after `D:` was wiped and re-cloned): pure fact-recording, no sync attempted this run — flagged as a separate follow-up. Prior: `hermes@0e9fc2cc15` — **0 behind `upstream/main`** as of `RUN-2026-09-10-002`: `main` `git pull --rebase`d onto `origin/main` `23135479a2` (a +163 `NousResearch:main` sync — `fix(desktop)` plugin-SDK-lazy-import, clarify-form fixes, `fmt(js)`, and much more); the 2 local `NF-v0.10.0` commits (`CHG-2026-09-10-001`/`-002`) replayed **0 conflicts**. `RUN-2026-09-10-003` then added the `ERR-2026-09-10-001` fix + the handoff-line check (`CHG-2026-09-10-003`/`-004`, `NF-v0.10.1`) and **pushed all of it to `origin/main`** — the review-before-push hold, in place since `NF-v0.10.0`, released by the owner. Base coordinate (`merge-base HEAD upstream/main`) unchanged — still `hermes@0e9fc2cc15`. Prior: **0 behind `upstream/main`** as of `RUN-2026-09-09-001` (`NF-v0.8.2`): the 2 code commits rebased clean onto `origin/main` `4d2bb83617` (twice — a second advance `f12feace3a` → `4d2bb83617` arrived between rebase and push), which already contained `upstream/main`. Prior: `hermes@c076d653a2` — **0 behind `upstream/main`** as of `RUN-2026-09-08-004`: the pass started level with `origin/main` on base `520e63661c`; `origin/main` then advanced +33 (a `NousResearch:main` merge `8409e486d8` — desktop / bot-mode / gateway + `fmt(js)` + a copilot GH-Actions fix). The 7 `NF-v0.8.0` commits rebased **clean, 0 conflicts** (none of the 33 touch a `CHG-2026-09-08-008..016` file); coordinate stamped at the rebase. Prior: `hermes@2237be3559` — **11 behind `upstream/main`** as of `RUN-2026-09-08-001` (`git fetch upstream`): local `main` had not moved since `NF-v0.6.1`; `origin/main` (0 behind) absorbed an 11-commit upstream sync — model-discovery / `key_cmd` picker / Gemini-schema / TUI extended-key / cron-DM fixes. `NF-v0.7.0` is committed on local `main` on this base, **not** rebased onto `origin/main` and not pushed. Prior: **0 behind** as of `RUN-2026-09-07-010`: `origin/main` advanced `13fd25e063` → `a4ffbca513` mid-run (owner's `Merge branch 'NousResearch:main'` upstream sync + a `fmt(js)` pass); `NF-v0.6.0` rebased **clean** onto it (no file overlap). Prior: `hermes@03f3b09222` — **47 behind** as of `RUN-2026-09-07-008`. `origin/main` advanced +9 mid-run (`e288a7f0c5` `Merge branch 'NousResearch:main'`, an upstream sync); this run's commit rebased **clean** onto it (no file overlap), moving the base from `hermes@233757037d (8 behind)`. Earlier: **6 behind** as of `RUN-2026-09-07-004` after `origin/main` advanced +15 via `c4e88d2ab6`; `RUN-2026-09-07-003` rebase replayed the NF commits (333→0 behind) onto `a7198a8855`. | 2026-09-11 |
| North-Forge version | `NF-v0.11.2` (`RUN-2026-09-11-002`; `CHG-2026-09-11-003`/`-004`): incident response — ERR-2026-09-11-001 (GitHub fork-sync force-reset `origin/main` to upstream, discarding all 186 fork commits, 2nd occurrence). Branch protection enabled on `main` (`allow_force_pushes:false`, `allow_fork_syncing:false`, `enforce_admins:true`); `.github/workflows/nf-branding-guard.yml` added as a detection layer; local `main` force-pushed back to `origin/main` to recover. See DECISION-2026-09-11-001. Prior: `NF-v0.11.1` (`RUN-2026-09-11-001`; `CHG-2026-09-11-002`): docs-only PATCH — `docs/BUILDING-A-DRIVE.md` gains a tested "Pinning to a private edition" walkthrough after a real end-to-end run (bootstrap → `nf-setup.ps1 -Pin kyocera` → verified `hermes profile list`/`/edition`/`nf_tier show`, all real, not just code-read); verification artifacts deleted after. Prior: `NF-v0.11.0` (`RUN-2026-09-11-001`; `CHG-2026-09-11-001`): **`private-editions/` discovery path for named vertical skill-sets.** MINOR — new capability, no access-tier enforcement change. `.gitignore` gains `private-editions/` (whole subtree, never tracked); `scripts/nf-setup.ps1`'s per-edition install-source resolution now falls back to `private-editions/<name>/` when `editions/<name>/` has no `distribution.yaml`; `editions/README.md` documents the mechanism. Verified against a real local clone (`private-editions/kyocera/`, populated from `kwalker7631/north-forge-hermes-edition` after that repo's own retirement-of-standalone-launcher restructuring): `hermes profile install private-editions/kyocera -y` succeeds end-to-end in an isolated scratch `HERMES_HOME` (`hermes profile list`/`info` show `kyocera@0.1.0`); `git status`/`git add -A` confirm the ignore holds; `/edition` and `nf_tier.enforce_startup_profile` need no change (both already disk-scan/profile-generic). Report: `D:\logs\PRIVATE-EDITIONS-DISCOVERY_2026-09-11.md`. Prior: `NF-v0.10.2` (`RUN-2026-09-10-006`; `CHG-2026-09-10-005`): **logging-pipeline sanity check.** Part 1: confirmed `D:\logs.zip` (script-generated, `collect-logs.{ps1,sh}`) and the drive-root `HANDOFF_<date>_<time>.zip` (currently a manual copy-and-seal step, no script backs it) are two separate things; `logs.zip` verified regenerating correctly (7 distinct sha256 across 7 runs tonight) — not broken, just not the file to re-upload. Part 2: re-confirmed `check_handoff_line()` already correctly FAILs both real-world unfilled phrasings the owner cited ("HANDOFF_PENDING.zip (sha256: PENDING)", "sha256: computed after the zip is sealed…") — they exist only as intentionally-frozen pre-hash copies *inside* the two already-sealed `HANDOFF_*.zip` bundles, never reachable by the completeness check (which only reads the standalone `D:\logs\` copies); removed a now-visibly-dead word-based clause, hardened the module comment, `+3` regression tests (12 total), and honestly documented one new residual limitation (a well-formed-but-fabricated hash still passes — format-only check, no drive-root cross-check). Also fixed in passing: `NF-v0.10.1`'s CHANGELOG block had landed *below* `NF-v0.10.0` (against the documented newest-first order) — reordered, pure move, no content change. PATCH — no functional behaviour change. Report: `D:\logs\LOGGING-PIPELINE-SANITY-CHECK_2026-09-10.md`. Prior: `NF-v0.10.1` (`RUN-2026-09-10-003`; `CHG-2026-09-10-003`/`-004`): **[`-003`] restored `def _desktop_ssh_backend` verbatim from `677e8ed8a4` — a merge had dropped it from `hermes_cli/main.py` while keeping the module-scope call, so `import hermes_cli.main` raised `NameError` on an unprovisioned / Full-tier drive; closes `ERR-2026-09-10-001` (HIGH). [`-004`] the RUN-to-report completeness check now FAILs a session report whose mandatory closing `Handoff bundle:` line is still a `PLACEHOLDER` template (`scripts/lib/report_completeness.py` + `SESSION-REPORT-TEMPLATE.md` + `logs/ledger/README.md` + `tests/test_report_completeness_handoff_line.py` ×7).** PATCH — a correctness fix + a ledger-tooling fix; no schema / engine / access-tier behaviour change. Verified (`import hermes_cli.main` OK; `test_nf_admin` 17p; `test_nf_tier_enforcement` 30p, prior 1 failure gone; `test_apply_profile_override` 8p / 3 pre-existing Windows-only fails; new handoff-line tests 7p; `report_completeness.py` still exits 0 on the live ledger), then **pushed to `origin/main` with the 4 held `NF-v0.10.0` commits as one owner-authorised batch**. Prior: `NF-v0.10.0` (`RUN-2026-09-10-001` + `RUN-2026-09-10-002`; `CHG-2026-09-10-001`/`-002`, `DECISION-2026-09-10-001`): **[`-001`] North Forge Full-tier in-session admin trigger — a Full-tier operator types the drive's admin passcode as a bare message to re-open `nf-setup.ps1` (tier / pin / edition) without quitting. [`-002`] Branding-drift correction — README / translations / tease page now lead with North Forge as the identity, Hermes Agent credited as the engine it is *built on* (attribution, `hermes` command, badges, docs links, dual LICENSE all unchanged; consistent with `BRANDING.md` §1 + `DECISION-2026-09-07-002`).** MINOR overall (new admin capability; no schema/engine/enforcement change; `-002` is PATCH-level docs). Rebased onto `origin/main` `23135479a2` (+163, 0 conflicts) `RUN-2026-09-10-002`; `+2 / 0`, not pushed; backup at `D:\NF-v0.10.0-BACKUP_2026-09-10\`. New `hermes_cli/nf_admin.py`  New `hermes_cli/nf_admin.py` (`maybe_recognize_admin_phrase` — Full-tier + passcode-set + exact-match only; silent + indistinguishable from normal chat on every other input; hits/plausible-misses logged for the owner without the passcode) + `nf_tier.log_admin_attempt`/`admin_attempt_log_path`; `run_nf_reconfig_and_resume` defers to `nf-setup.ps1 -Force` after teardown then re-execs `hermes`. Completed an uncommitted WIP: added the missing `-Force` (the launch path refused every already-provisioned drive without it — auth unchanged), a test-only `root=` seam, and reassigned the placeholder id `CHG-2026-09-09-004`→`CHG-2026-09-10-001`. `tests/test_nf_admin.py` +17. **Committed to local `main`; pushed to `origin/main` in `RUN-2026-09-10-003`.** Found in passing here, fixed in `RUN-2026-09-10-003` (`CHG-2026-09-10-003`): `ERR-2026-09-10-001` — a merge dropped `def _desktop_ssh_backend` from `hermes_cli/main.py`, `import hermes_cli.main` → `NameError`. Report: `D:\logs\IN-SESSION-ADMIN-TRIGGER_2026-09-10.md`. Prior: `NF-v0.9.0` (`RUN-2026-09-09-002`, `CHG-2026-09-09-003`, `DECISION-2026-09-09-001`): **drives can now carry their own Python toolchain — `bootstrap-north-forge.ps1` builds its venv with nothing on the recipient's `PATH` and no network.** MINOR (new drive-provisioning capability + admin workflow; no schema/engine change; no change to R1). `DECISION-2026-09-09-001` chose **B**: bundle `uv.exe` + a `python-build-standalone` CPython 3.11 as a **never-git-tracked drive sibling `<parent>\<leaf>-toolchain\`**, admin-prepared once and copied per drive (not fetched at first-launch, not git/LFS). New read-only `scripts/lib/nf-toolchain.ps1` (`Get-NfBundledToolchain`); `bootstrap` resolves **bundled → host PATH (admin/dev, logged `using host toolchain (admin/dev)`) → the existing verbatim error**; `uv venv --python <bundled python.exe>` is fully offline. New `docs/BUILDING-A-DRIVE.md` + `docs/toolchain/THIRD-PARTY-NOTICES.txt` (CPython/PSF, uv Apache-2.0-or-MIT, python-build-standalone). The `-toolchain` folder is an *input* to venv creation, never a venv — `nf-venv-state.ps1` / `nf-readiness.ps1` never look at it; a `-Force` rebuild still deletes only `-venv`. Tests: `test_bootstrap_north_forge_path_safety` +4 incl. a real **zero-PATH bundled build**; R1 state-matrix unchanged. Feasibility: `D:\logs\CODEX-BUNDLED-PYTHON-FEASIBILITY.md`. Prior: `NF-v0.8.2` (`RUN-2026-09-09-001`, `CHG-2026-09-09-001..002`): **landed two AGENT-D changes that were code-complete + locally verified but never committed (recovered from a `hermes-update` autostash dated 2026-09-09 01:42) — R1 bootstrap venv-cleanup + `session_listing.py` adaptive widening; two code commits + one ledger commit, rebased clean onto `origin/main`, pushed.** PATCH. `CHG-2026-09-09-001`: `bootstrap-north-forge.ps1` is now the single ownership-gated venv-cleanup authority — readiness (`Test-NfVenvReady`) and ownership (new read-only `scripts/lib/nf-venv-state.ps1` → `Get-NfVenvState`, 6 states) are separate decisions feeding a `create`/`rebuild`/`refuse`/`none` state table; `UnknownDirectory`/`UnsafePath` ⇒ unconditional refuse (`-Force` is a rebuild request, never a deletion override); the one `Remove-Item` is re-check-gated; the data-folder skin seed runs only on first-time `create`; new Venv-vs-Data overlap guard; `nf-preflight.ps1` reduced to a repair *request* (`action=bootstrap-repair`). Closes `ERR-2026-09-09-001` (partial/unrelated venv reused or deleted without ownership proof) + `ERR-2026-09-09-002` (Venv-vs-Data gap). `+13` tests. `CHG-2026-09-09-002`: `query_session_listing` widens `limit*(4,8,16)` from row 0 until `limit` displayable rows survive the visibility filter or the DB returns short — an older *named* session could otherwise hide behind newer unnamed rows in the fixed `limit*4` window (`ERR-2026-09-09-003`; same shape as `ERR-2026-09-08-008`); `limit<=0` ⇒ `[]` before any query; `+5` tests. This is the `session_listing.py` sparse-match hardening carried forward from `RUN-2026-09-08-005`. AGENT D's notes earmarked `ERR/CHG-2026-09-08-013/014/026/027`; reassigned to the 2026-09-09 sequence per the daily-`NNN`-reset rule. 68/68 targeted tests on the rebased tree. Prior: `NF-v0.8.1` (`RUN-2026-09-08-005`, `CHG-2026-09-08-017..025`): **consolidated commit pass — two batches of small correctness fixes landed together in dependency order, committed per-CHG, rebased onto `origin/main`, pushed.** PATCH. `CHG-2026-09-08-017`: `hermes_logging` handler-registration race — the dedup check + the append are now one critical section under `_queue_state_lock`, loser closed after release (`ERR-2026-09-08-005`; corrected per an independent review). `CHG-2026-09-08-018`: `nf-setup.ps1` drive test → `@pytest.mark.windows_only` so the CI Windows lane actually imports it (`ERR-2026-09-08-006`), + shared `tests/_windows_env.py::minimal_windows_subprocess_env()` (was duplicated in 2 files). `CHG-2026-09-08-019`: `worktree_gc._archive_untracked()` archives under `get_hermes_home()`, not a hardcoded `~/.hermes` (`ERR-2026-09-08-007`). `CHG-2026-09-08-020`: `CONTRIBUTING.md` cross-platform testing policy — canonical host-OS markers, no faking process-global host identity, extract pure decision fns. `CHG-2026-09-08-021`: record-aware `hermes logs` filtering — `_iter_records()` groups header+continuations, filter on the header only, filtered `_read_tail` streams the whole file into `deque(maxlen=N)` (fixes both the ~2000-line sparse-match miss and the per-line multiline leak/drop; `ERR-2026-09-08-008`; `+12` tests). `CHG-2026-09-08-022`: `cron.load_jobs()` persists `{"jobs": []}` even when the repair is empty (`ERR-2026-09-08-009`). `CHG-2026-09-08-023`: `auth_model_picker._confirm_selection_guards()` fails **closed** on an unexpected registry error (`ERR-2026-09-08-010`). `CHG-2026-09-08-024`: `voice.py` comment-only. `CHG-2026-09-08-025`: `hermes_time.get_timezone()` re-checks the cache identity after the outside-lock resolve and retries on a shift (`ERR-2026-09-08-011`). Opened OPEN: `ERR-2026-09-08-012` (`nf-setup.ps1` null-`$LASTEXITCODE` → `Provisioning failed (exit )`; fails safe; access-tier code, not self-fixed). `session_listing.py` sparse-match hardening scoped but **not started** — carried forward. Verification: targeted suites green (`test_hermes_logging` 28+1skip / **2 pre-existing fails**, `test_logs` 26, `test_worktree_gc` 17, `test_nf_tier_enforcement` 30, `test_bootstrap_launcher_hardening` 13, `test_jobs` 113, `test_auth_model_picker_guards` 4, `test_hermes_time_cache` 2, OS-marker tooling 13); a full `run_tests.sh` was stopped — it fights git for the index lock and the fork carries a large unrelated pre-existing failure baseline (`tests/acp/**`, `tests/agent/**`). Prior: `NF-v0.8.0` (`RUN-2026-09-08-004`, `CHG-2026-09-08-008..016`): **hygiene / cleanup pass + a second Codex progress-check batch — committed per-CHG and pushed.** `CHG-2026-09-08-008`: relational RUN-to-report completeness check — new `scripts/lib/report_completeness.py` + `logs/ledger/reports/REPORT-MANIFEST.md` (every ledger `RUN-` id must map to a report or an explicit ledger-only line; missing/mismatched ⇒ **FAIL**), `SESSION-REPORT-TEMPLATE.md`, both collectors rewired, 4 un-reported runs backfilled (closes `ERR-2026-09-07-004`). `CHG-2026-09-08-009`: `.githooks/secret-guard` filename blocklist broadened to SSH keys / `*.p12`·`*.pfx`·keystores / `credentials.json`·SA-JSON / `.netrc`·`.pgpass`·`.htpasswd` / PEM·`.key` (public-bundle + `tests/` carve-outs, `ALLOWLIST` hatch), `+31` self-tests, CI runs both hook suites + `secret-guard --tree/--range` (closes the filename half of `ERR-2026-09-07-007`). `CHG-2026-09-08-010`/`-011`: overstated rebrand wording softened in README ×2 + 3 translations + BRANDING ×2; `cli.py` welcome fallback + `_parser.py` `chat` description moved to North Forge; BRANDING now names the deliberately-Hermes surfaces (closes `DECISION-2026-09-07-002`). `CHG-2026-09-08-012`: README install section now leads with **North Forge Setup** (`North-Forge-Setup.exe`, double-click) for non-technical recipients, `north-forge.cmd` demoted to advanced. `CHG-2026-09-08-013`: `nf_tier.load()` made total over arbitrary signed input (was `int("one")` → `ValueError` escaping → enforcement silently off); `_nf_tier_gate` fails **closed** when a record file is present; `+11` tests (closes `ERR-2026-09-08-001` / Codex `PC-2026-09-08-001`). `CHG-2026-09-08-014`: `apps/bootstrap-installer` — one shared `repo::validate_target` gate for the picker **and** the pre-spawn path, autodetect only when the global checkout count is exactly 1; `+8` Rust tests (closes `ERR-2026-09-08-002` / `PC-2026-09-08-002`+`-003`). `CHG-2026-09-08-015`: `editions/pine-barron-farms` canon loader — `canon/load_canon.py` (receipt: path + sha256 + bytes, or a not-found warning) + the new `skills/pbf-canon/` skill + a SOUL-mandated session-start command; no engine change; `tests/test_pbf_canon_loader.py` (6, incl. an assembled-context proof) (closes `ERR-2026-09-08-003` / `PC-2026-09-08-004`). `CHG-2026-09-08-016`: `editions/penny-pincher` persona wording narrowed — "I write it down", "no ledger of its own", affordability answers name what they omit (closes `ERR-2026-09-08-004` / `PC-2026-09-08-005`). `ERR-2026-09-06-001` (the live `.env` key) left untouched — accepted, time-limited, owner-decided. Prior: `NF-v0.7.0` (`RUN-2026-09-08-001` + `RUN-2026-09-08-002`, `CHG-2026-09-08-001..005`): **North Forge Setup GUI + enforced first-launch skin + docs sweep + tease page** — pushed in `RUN-2026-09-08-003` (`CHG-2026-09-08-006..007`). `CHG-2026-09-08-001`: `apps/bootstrap-installer/` retargeted from the Hermes machine-local installer to a branded GUI that shells `bootstrap-north-forge.ps1` unmodified — rebrand + strip the stage-protocol / `update.rs` / GitHub-download machinery / Electron path, new `repo.rs` checkout+drive resolution, single indeterminate progress, `location.tsx` picker, "Launch" runs `north-forge.cmd`; `tauri build` → `North-Forge-Setup.exe`, verified fresh + already-bootstrapped against `D:\north-forge-agent`. `bootstrap-north-forge.ps1` / `nf-preflight.ps1` untouched. `CHG-2026-09-08-002`: `config._ensure_default_skin` (seeded at `initialize_home`, mirrors `_ensure_default_soul_md`) + `nf-setup.ps1` now pin `display.skin = north-forge` on a fresh fork-seeded home; explicit choices kept; `+7` tests. `CHG-2026-09-08-003`: `logs/ledger/README.md` version line de-hardcoded (was stale `NF-v0.1.0`); `README.md` "Drive class" reworded to match reality (no volume-label code; points at `nf-setup.ps1 -Show`). `CHG-2026-09-08-004`: `docs/preview.html` single-file tease page — **superseded by `CHG-2026-09-08-005`** (`RUN-2026-09-08-002`), which rebuilt the page on the real brand art (`assets/banner.png` hero + `assets/icons/splash-alt.png` + favicon, embedded as `data:` URIs; no network loads, no JS; headless-Chrome render verified). `NF-v0.6.1` (`RUN-2026-09-07-011`, `CHG-2026-09-07-023`): **ledger-only — two long-standing decisions ratified (no tracked-file change outside `logs/ledger/`).** `DECISION-2026-09-06-003` (install model) → **DECIDED — A (drive-native run-in-place)**: the sibling-venv layout + launch-readiness probe (`nf-preflight.ps1`) + tier/pin (`nf_tier.py`) stack (`NF-v0.5.1`→`NF-v0.6.0`, on `CHG-2026-09-07-005`) already is a working, tested implementation of exactly that model — confirmed in-tree and by two independent Codex audits; B (machine-local) / C (hybrid) rejected; the parked hardened form (drive seal / dual-volume `NORTHFORGE`+`NORTHFORGE-DATA` / certify-verify-audit) is now **unblocked but not mandated**. `DECISION-2026-09-06-002` (attic clone) → **DECIDED — A (delete)**; `origin/main` carries everything, zero unique commits — physical `Remove-Item` of `D:\north-forge-agent-attic\nested-clone-2026-09-06\` recorded as **owed, low urgency**. `NF-v0.6.0` (`RUN-2026-09-07-010`, `CHG-2026-09-07-022`): **two-tier access model + pinned front-door edition** — the tier/pin mechanism the architecture notes flagged as the biggest blocker. An *edition* is a Hermes profile (`DECISION-2026-09-07-003` DECIDED). New `hermes_cli/nf_tier.py` reads a signed `<nf-root>/north-forge/provisioning.json` (HMAC-SHA256, key on-drive — *tamper-evident, not tamper-proof*, stated plainly; absent record ⇒ un-provisioned ⇒ plain-Hermes behaviour; unverifiable record ⇒ drive refuses to start). **Basic tier** is locked to its pin at the profile-resolution layer: `_apply_profile_override` forces the pin and **exits non-zero** on an explicit `-p <other>` / foreign `HERMES_HOME` (never a silent redirect); backstops in `resolve_profile_env` + `set_active_profile`; `profile_cmd` mutations refused; dashboard `GET/POST /api/profiles*` filtered / 403; new `/edition` command. **Full tier** = pin is a default, switcher stays open. New `scripts/nf-setup.ps1` (Setup Run wizard — a deliberate one-time admin step, PS-5.1 stderr-safe); `bootstrap` + `north-forge.cmd` gain a provisioning check. `+tests/test_nf_tier_enforcement.py` (19). Role mapping validated. `NF-v0.5.5` (`RUN-2026-09-07-009`, `CHG-2026-09-07-021`): **CLI skin full re-theme (v1 of rebuild)** — `skins/north-forge.yaml` goes from a branding/art swap to a full themed session (30-key `colors:` block, `spinner:` block, `tool_prefix`, a Braille-pattern compass+anvil+flame `banner_hero`) at `charizard`-level coverage; engine unmodified; palette passes the built-in WCAG audit. Preview + refinement list in `D:\logs\SKIN-REBUILD-V2_2026-09-07.md`. `NF-v0.5.4` (`RUN-2026-09-07-008`, `CHG-2026-09-07-020`): **critical launcher fix** — `north-forge.cmd` + `bootstrap-north-forge.ps1` stopped trusting "`hermes.exe` exists" as readiness (`ERR-2026-09-07-006`, reclassified CRITICAL after a real first-handoff failure; Codex `F-05` / `R-03.3`). New `scripts/lib/nf-readiness.ps1` runs a four-check launch-time probe (python runs / `import hermes_cli` / module resolves in-checkout / `.nf-bootstrapped` `repo=` matches); new `scripts/nf-preflight.ps1` silently rebuilds the **venv only** (data untouched) on any failure and writes one launcher-log line per launch before Python starts. `+tests/test_nf_preflight_readiness.py` (10). `NF-v0.5.3` (`RUN-2026-09-07-007`, `CHG-2026-09-07-019`): **security fix** — `.githooks/content-scan` `--commits` gate no longer word-splits changed paths (Codex `F-03`, `ERR-2026-09-07-005`); it reads `git diff-tree -z` and scans by post-image blob OID. A secret in a space/tab/dash/Unicode filename was passing pre-push **and** CI; +7 `run.sh` cases. `NF-v0.5.2` (`RUN-2026-09-07-006`, `CHG-2026-09-07-016..018`): two cheap Codex-audit fixes — README stale-MinGit prose removed / linked upstream (`F-08`); `nf-secret-scan.yml` `actions/checkout` pinned to SHA (`F-10`) — plus ledger back-logging of Codex `F-02`/`F-05`/`F-07`/`F-09` (`ERR-2026-09-07-004`/`006`/`007`, `DECISION-2026-09-07-002`). No behavioural code change. `NF-v0.5.1` (`RUN-2026-09-07-005`, `CHG-2026-09-07-015`): **data-loss fix** — `bootstrap-north-forge.ps1` path guard rejects a venv/data dir equal to / inside / containing the checkout (Codex `F-04`, `ERR-2026-09-07-003`). `NF-v0.5.0` (`RUN-2026-09-07-004`, `CHG-2026-09-07-011..014`): README Quick Install restructure (drive-native `north-forge.cmd` first); North Forge CLI skin (`skins/north-forge.yaml`, `DECISION-2026-09-07-001` **DECIDED — skin swap stands**); self-healing `<drive>:\Start North Forge.lnk` launcher. `NF-v0.4.2` (`CHG-2026-09-07-010`: `upstream/main` sync). `NF-v0.4.1` (`CHG-2026-09-07-009`). `NF-v0.4.0` first application-code identity pass. `NF-v0.2.0`..`NF-v0.7.0` public on `origin/main`. `NF-v0.8.0`..`NF-v0.10.1` all pushed (`NF-v0.10.0` + `NF-v0.10.1` pushed together in `RUN-2026-09-10-003`). | 2026-09-11 |
| Ledger schema | `v2` | 2026-09-06 |
| Latest run | `RUN-2026-09-11-002` — **ERR-2026-09-11-001 fork-sync-reset incident response.** Diagnosed GitHub fork-sync force-resetting `origin/main` to upstream (2nd occurrence, 186 fork commits lost), recovered via force-push, enabled branch protection (incl. `allow_fork_syncing:false`), added `nf-branding-guard.yml` CI check. `NF-v0.11.2`. This row lagged behind the ledger before this update — `RUN-2026-09-11-001` (private-editions discovery) and `RUN-2026-09-10-007` were not reflected here in turn; not backfilled, out of this run's scope. Prior recorded: `RUN-2026-09-10-007` — **ledger-only: stub honesty audit, no code change.** Goal: nothing a curious demo-drive user pokes at should dead-end into a crash, silent no-op, or broken-looking result. Inventoried every edition/major feature and actually invoked what has real code: `editions/pine-barron-farms/canon/load_canon.py` in a scratch dir with no canon packet present — exit 0, clear `WARNING: canon packet NOT FOUND` message, no crash; `hermes_cli.slash_exec._exec_edition()` against a scratch unprovisioned `HERMES_HOME` — clean honest message, no exception. Grepped `hermes_cli/`/`agent/`/`tools/` for admin-console / skill-gating code — zero hits, confirming the in-session admin trigger genuinely has no such feature to be a stub (not broken, absent). Re-confirmed `hermes-webui` is still zero-integration (grep for "north forge" across every `.ts`/`.tsx`/`.json` — zero matches) and Pocket Penny / field-service carry no skill code at all (persona-only, `SOUL.md` never oversells tax-categorization/reports it doesn't do). **Result: no fixes needed anywhere in this repo** — every genuinely testable code path (canon loader, `/edition`, admin-trigger failure branches) already produces a plain honest message and exits cleanly; the "not yet built" items simply have no entry point to dead-end on. One real, unrelated gap (Kyocera `/humanizer` in `north-forge-hermes-edition`'s `TRY_THIS_CARD.md`) was already flagged in that repo's own audit trail last session and explicitly out of scope to touch here. Report: `D:\logs\STUB-HONESTY-AUDIT_2026-09-10.md`. Prior: `RUN-2026-09-10-006` — **logging-pipeline sanity check, `NF-v0.10.2`.** Owner noticed `logs.zip` uploaded with an identical sha256 three times running while dated `HANDOFF_*.zip` files each differed, and asked whether the completeness gate had let two reports through with unfilled handoff lines that dodged the literal word `PLACEHOLDER`. Findings: (1) `logs.zip` (`collect-logs.{ps1,sh}`) and `HANDOFF_<date>_<time>.zip` (currently 100% manual, no script backs it) are separate artifacts; `logs.zip` verified regenerating correctly — 7 distinct sha256 values across 7 real runs tonight — so the "identical 3x" symptom is a process gap (a stale grab, or a past session skipping the standing collect-logs step), not a broken script; recommended (not built) documenting `logs.zip` as internal-only and scripting the dated-copy step. (2) The two cited unfilled phrasings ("HANDOFF_PENDING.zip (sha256: PENDING)", "sha256: computed after the zip is sealed…") never reached the completeness gate at all — they're the intentionally-frozen pre-hash copies *inside* the already-sealed `HANDOFF_2026-09-10_2240.zip`/`_2246.zip` bundles; the gate only ever validates the standalone `D:\logs\` files, both of which already show the real, filled-in values and PASS. Tested `check_handoff_line()` directly against both exact phrasings anyway: both already FAIL, correctly — `HEX64_RE` rejects non-hex regardless of wording, not a word-blacklist. `CHG-2026-09-10-005`: removed the one now-visibly-dead word clause, hardened the doc comment, `+3` tests (12 total, incl. one new honestly-disclosed limitation: a well-formed-but-fabricated hash still passes — format-only, no disk cross-check). Also fixed in passing: `NF-v0.10.1`'s CHANGELOG block had landed below `NF-v0.10.0`, against newest-first order — reordered (pure move). Report: `D:\logs\LOGGING-PIPELINE-SANITY-CHECK_2026-09-10.md`. Prior: `RUN-2026-09-10-005` — **ledger-only: verified there is NO automatic launch-time `git pull`, anywhere.** Owner asked to confirm a specific feature (pull-and-continue before the venv/bootstrap check). Traced all 3 entry points in execution order — `north-forge.cmd` (rename-recovery → `nf-preflight.ps1` readiness/venv-self-heal → data-folder creation → skin-copy/shortcut self-heal → `nf_tier verify` → launch `hermes.exe`), `scripts\bootstrap-north-forge.ps1` (+ its 3 dot-sourced `lib\` scripts), and `apps\bootstrap-installer\src-tauri\src\*.rs` (shells `bootstrap-north-forge.ps1` once) — grepped each for `git pull`/`fetch`/`rebase`/`merge`: **zero matches** in any of them. The only two `git` mentions found are comments (`north-forge.cmd:107`, `lib.rs:8-9`) documenting that updates are meant to arrive via a *manual* `git pull` (by an admin/agent) followed by the *existing* self-heal machinery picking it up on the next launch — not an automatic pull the launcher performs itself. Distinguished from `scripts/install.ps1`/`.sh`, which DO run real `git fetch`/`pull` but are the rejected upstream machine-local installer, never invoked by the drive-native launch chain. No changes made; reported the gap + 4 open design questions (fail-open vs fail-closed, every-launch vs periodic, silent vs messaged, local-edit safety) for a follow-up. Report: `D:\logs\VERIFY-LAUNCH-GIT-PULL_2026-09-10.md`. Prior: `RUN-2026-09-10-004` — **ledger-only: persona-mechanism investigation + backup cleanup, no tracked-file change.** (1) Investigated `/personality` for Pocket Penny persona design (`POCKET_PENNY_SPEC.md` context, report only): **per-edition scoped** (`display.personality` lives in the active profile's `config.yaml`, resolved via `HERMES_HOME`; editions are profiles); ships with **13 built-in personas** (`hermes_cli/personality.py::BUILTIN_PERSONALITIES`) plus edition/user `agent.personalities` overlays; **layers on top of `SOUL.md`**, never overrides it — `SOUL.md` sits in the cached stable prompt tier, the active personality's text is appended at API-call time only (`agent/chat_completion_helpers.py`, `agent/turn_context.py`), never cached. Noted in passing (already fixed upstream, not touched): `tests/tui_gateway/test_personality_clobbers_system_prompt.py` (2 passed) documents a since-fixed desktop-RPC bug that once conflated the personality overlay with the separate global `agent.system_prompt`. (2) Re-confirmed `D:\NF-v0.10.0-BACKUP_2026-09-10\` redundant — `main`/`origin/main` still `0/0` at `076d8f4e3c`, every `NF-v0.10.0`/`NF-v0.10.1` commit confirmed on `origin/main` (`git branch -r --contains`) — then **deleted it** (144 KB). Left alone: the `nf-v0.10.0-hold` branch/tag still in the repo (flagged, not pruned). Report: `D:\logs\PERSONA-CHECK-AND-BACKUP-CLEANUP_2026-09-10.md`. Prior: `RUN-2026-09-10-003` — **three fixes in one pass, then the push.** (1) Restored `def _desktop_ssh_backend(argv)` **verbatim from `677e8ed8a4`** to `hermes_cli/main.py` (before `_apply_profile_override`, its position in that commit) — a `Merge branch 'main' into main` had dropped the `def` while keeping the module-scope call, so `import hermes_cli.main` raised `NameError` on an unprovisioned / Full-tier drive. `import hermes_cli.main` now OK; the covering test (`test_apply_profile_override.py::…::test_desktop_ssh_serve_child_skips_active_profile`) survived the merge and passes; `test_nf_admin` **17p**; `test_nf_tier_enforcement` **30p** (the prior 1 failure — `test_integration_full_defaults_to_pin_but_switches` — is gone); `test_apply_profile_override` **8p / 3 pre-existing Windows-only fails** (proven identical on the untouched tree). Closes `ERR-2026-09-10-001` (HIGH), `CHG-2026-09-10-003`. (2) `scripts/lib/report_completeness.py` gains `check_handoff_line()`: a session report dated `2026-09-10`+ whose mandatory closing `Handoff bundle:` line is missing, or still reads `HANDOFF_..._PLACEHOLDER.zip (sha256: PLACEHOLDER)`, or carries a non-64-hex sha, is now a **FAIL** — so `collect-logs.{ps1,sh}` won't report `COMPLETE` on a template artifact (the bug in `RUN-2026-09-10-001`/`-002`'s handed-over reports). `- NOT created (reason)` accepted; prose "placeholder" doesn't trip it; pre-`2026-09-10` reports not retroactively failed. `SESSION-REPORT-TEMPLATE.md` + `logs/ledger/README.md` "End-of-task handoff" updated; `+tests/test_report_completeness_handoff_line.py` (7). `CHG-2026-09-10-004`. (3) **Pushed** the 4 held `NF-v0.10.0` commits (`CHG-2026-09-10-001`/`-002` + their ledger commits) plus this run's fixes to `origin/main` `23135479a2..<head>` — the review-before-push hold, held since `NF-v0.10.0`, released by the owner. Report: `D:\logs\CRASH-FIX-HANDOFF-LINE-AND-PUSH_2026-09-10.md`. Prior: `RUN-2026-09-10-002` — **drive-copy cleanup + Hermes-forward branding-drift correction.** *Drive cleanup (per owner Q&A):* backed up the 2 unpushed `NF-v0.10.0` commits 4 ways (`branch nf-v0.10.0-hold`, tag, thin bundle, `format-patch` → `D:\NF-v0.10.0-BACKUP_2026-09-10\`), then `git pull --rebase`d `D:\north-forge-agent` onto `origin/main` `23135479a2` (+163 `NousResearch:main` sync, **0 conflicts**; `test_nf_admin` 17/17 on the rebased tree; `+2 / 0`, still not pushed). `git pull` brought `D:\north-forge-hermes-edition` current (`1c47b60..bb4838d`, `0/0`). `D:\hermes-webui` already current (`e168b67e`, `0/0`). Removed stale non-git `D:\north-forge-agent-main` (12,221 files, all mtime 2026-09-07, `NF-v0.6.0`-era, superseded). **Flagged back, kept:** `D:\north-forge-agent-attic\marguerite-and-penny-suno.txt` (unique personal file). **Left untouched (owner):** all of `E:\` (a live session ran there 18:56 today), `D:\north-forge-agent-data`/`-venv`. *Branding (`CHG-2026-09-10-002`, PATCH):* `README.md` + 3 translations + `docs/preview.html` reframed North-Forge-first; Hermes credited as the *engine built on*, not the identity; 3 stale `used unmodified` phrasings aligned to `DECISION-2026-09-07-002`; `hermes` command / badges / docs links / `pyproject` name / dual LICENSE deliberately unchanged. *Investigation (reported, no change):* the in-session admin trigger is a passcode→`nf-setup.ps1` reconfiguration shortcut, **not** an in-session admin console and **not** passcode-gated skill install/enable (both are gaps vs. the ask); `hermes-webui` is a standalone clone with **zero** wiring to `north-forge-agent`; editions = `field-service` / `penny-pincher` (persona-only) / `pine-barron-farms` (persona + canon-loader skill), **no Kyocera** (kept out of the chassis by design — lives in `D:\north-forge-hermes-edition`). `ERR-2026-09-10-001` reconfirmed present on `origin/main` post-rebase. Report: `D:\logs\DRIVE-CLEANUP-AND-BRANDING_2026-09-10.md`. Prior: `RUN-2026-09-10-001` — **completed + landed the North Forge Full-tier in-session admin trigger** (`CHG-2026-09-10-001` / `NF-v0.10.0`, `DECISION-2026-09-10-001` owner-directed, opened + decided same run). The passcode-as-bare-message recogniser (`hermes_cli/nf_admin.py`) + owner-review attempt log (`nf_tier.log_admin_attempt`) + `nf-setup.ps1 -Force` deferral. The feature arrived as an uncommitted WIP; completing it fixed a non-functional launch path (missing `-Force` → `nf-setup.ps1` refused every already-provisioned drive; auth unchanged), added a `root=` test seam, and reassigned `CHG-2026-09-09-004`→`CHG-2026-09-10-001`. One code commit + one `ledger:` commit on base `hermes@0e9fc2cc15` (`NF-v0.9.0` HEAD `bb64aab4f3`). **Not pushed** — local `main` is 4 behind `origin/main` (`NousResearch:main` sync + `fmt(js)` + 2 `fix(desktop)`); rebase + push is the owner's call. Opened OPEN: `ERR-2026-09-10-001` (HIGH — merge dropped `def _desktop_ssh_backend` from `hermes_cli/main.py`, kept the call → `import hermes_cli.main` `NameError` on an unprovisioned / Full-tier drive; on `origin/main` too; not self-fixed — access-tier code, out of scope). Verification (`.venv` pytest, `TZ=UTC PYTHONHASHSEED=0`): `test_nf_admin` **17 passed** (new), `test_nf_tier_enforcement` **29 passed / 1 failed** — the 1 is `ERR-2026-09-10-001`, reproduced on the reverted tree; `ruff check` clean. Report: `D:\logs\IN-SESSION-ADMIN-TRIGGER_2026-09-10.md`. Prior: `RUN-2026-09-09-002` — **`DECISION-2026-09-09-001` decided + implemented: drive-native bundled Python toolchain.** Chose **B** (bundle `uv.exe` + a `python-build-standalone` CPython 3.11 as a never-git-tracked drive sibling `<parent>\<leaf>-toolchain\`, admin-prepared + copied per drive). New read-only `scripts/lib/nf-toolchain.ps1` (`Get-NfBundledToolchain`); `bootstrap-north-forge.ps1` venv-creation branch resolves **bundled → host PATH (logged `using host toolchain (admin/dev)`) → the existing verbatim error** — R1's ownership classifier / readiness probe / state table untouched (the `-toolchain` folder is an input to venv creation, never a venv). `docs/BUILDING-A-DRIVE.md` + `docs/toolchain/THIRD-PARTY-NOTICES.txt` added. `NF-v0.9.0` / `CHG-2026-09-09-003`. One code commit + one `ledger:` commit on base `hermes@a60c74b52a`, `git pull --rebase origin main`, pushed. Verification (Windows-native, `scripts/run_tests.sh` targeted): `test_bootstrap_north_forge_path_safety` 29 (25 + 4 toolchain, incl. a real zero-PATH bundled build — staged uv + a junction to a `python-build-standalone` CPython, PATH scrubbed), `test_bootstrap_launcher_hardening` 15, `test_nf_preflight_readiness` 10, `test_session_listing` 18 — no R1 regression. Report: `D:\logs\NF-v0.9.0-BUNDLED-TOOLCHAIN_2026-09-09.md`. Prior: `RUN-2026-09-09-001` — **landed AGENT D's uncommitted R1 + session-listing work.** A prior read-only pass found both changes code-complete and locally verified but never committed, surviving only in a `hermes-update` autostash (`stash@{0}`, `hermes-update-autostash-20260909-054201`, base == `NF-v0.8.1` HEAD `3e0ddfb05d`) with 7 modified tracked files + the new untracked `scripts/lib/nf-venv-state.ps1`; AGENT D's notes `D:\logs\NORTH-FORGE-R1-VENV-CLEANUP_2026-09-09.md` + `D:\logs\SESSION-LISTING-ADAPTIVE-WIDENING_2026-09-09.md` (no ledger entries yet). Restored the stash, split into two per-`CHG-` code commits + one `ledger:` commit, `git pull --rebase origin main` (2 code commits, **0 conflicts** over a +26 `origin/main` and then a further advance `f12feace3a` → `4d2bb83617` that landed between rebase and push — no incoming commit touches a changed file), pushed `4d2bb83617..71255db0a7` (2 code + 1 ledger commit). `NF-v0.8.2` / `CHG-2026-09-09-001..002`. Opened + resolved same run: `ERR-2026-09-09-001` + `ERR-2026-09-09-002` (R1 bootstrap venv ownership + Venv-vs-Data gap), `ERR-2026-09-09-003` (session-listing visibility filter). Verification on the rebased tree (`scripts/run_tests.sh` targeted): `test_bootstrap_north_forge_path_safety` 25, `test_nf_preflight_readiness` 10, `test_bootstrap_launcher_hardening` 15, `test_session_listing` 18 — **68/68**. Full `run_tests.sh` not run (index-lock contention + the standing `tests/acp/**` · `tests/agent/**` baseline). Report: `D:\logs\NF-v0.8.2-R1-AND-SESSION-LISTING_2026-09-09.md`. Prior: `RUN-2026-09-08-005` — **consolidated commit pass: the reconciliation-corrections batch + the focused-correctness batch + the remaining pre-existing local work, nine code commits + one ledger commit in dependency order, `NF-v0.8.1` / `CHG-2026-09-08-017..025`.** Committed on base `hermes@7ee2b69151`, then `git pull --rebase origin main` (conflict-free, as the earlier reconciliation analysis predicted) and pushed. Opened + resolved same run `ERR-2026-09-08-005..011`; opened OPEN `ERR-2026-09-08-012` (`nf-setup.ps1` null-`$LASTEXITCODE`, deliberately not fixed — access-tier code). `session_listing.py` sparse-match hardening: scoped + read, **not started** (superseded mid-turn by the commit/push instruction) — carried forward. Verification: targeted suites green — `test_hermes_logging` 28 pass / 1 skip / **2 pre-existing fails** (`test_profile_routing_follows_context_home`, `test_managed_mode_initial_open_sets_group_writable` — managed-mode / Windows lane, reproduced with this batch reverted), `test_hermes_time_cache` 2, `test_bootstrap_launcher_hardening` 13, `test_nf_tier_enforcement` 30 (the now-`windows_only` PS drive executed on this win32 host), `test_worktree_gc` 17, `test_logs` 26, `test_jobs` 113, `test_auth_model_picker_guards` 4, `test_list_os_marked_tests` + `test_os_marker_gating` 13. A full `scripts/run_tests.sh` was **started then stopped** — it contends with concurrent `git` ops for `.git/index.lock`, and this fork carries a large pre-existing failure baseline (26 fails in the first ~9%, all `tests/acp/**` / `tests/agent/**` — none in any file this batch changed or any file importing a changed module). Report: `D:\logs\CONSOLIDATED-COMMIT-PASS_2026-09-08.md`. Prior: `RUN-2026-09-08-004` — **full hygiene / cleanup pass + a second Codex progress-check batch (`CODEX-PROGRESS-CHECK-2026-09-08.md`, supplied in the prompt, not on disk), committed per-CHG and pushed.** `NF-v0.8.0` / `CHG-2026-09-08-008..016`, 7 commits, rebased clean onto `origin/main` `8409e486d8` (base `hermes@c076d653a2`, 0 behind `upstream/main`). Closed: `ERR-2026-09-07-004` (relational RUN-to-report check — `scripts/lib/report_completeness.py` + `REPORT-MANIFEST.md`; 4 un-reported runs backfilled: `RUN-2026-09-07-011`, `RUN-2026-09-08-001`/`-002`/`-003`), `ERR-2026-09-07-007` filename half (`secret-guard` blocklist broadened + `+31` self-tests + CI wiring), `DECISION-2026-09-07-002` (rebrand wording softened everywhere + the two CLI surfaces moved to North Forge). Opened + resolved same run: `ERR-2026-09-08-001` (`nf_tier.load()` total over malformed-but-signed records; `_nf_tier_gate` fail-closed; `+11` tests), `ERR-2026-09-08-002` (installer `repo::validate_target` shared gate + no-silent-pick autodetect; `+8` Rust tests), `ERR-2026-09-08-003` (`pine-barron-farms` `load_canon.py` + `pbf-canon` skill + SOUL session-start mandate; `tests/test_pbf_canon_loader.py` ×6), `ERR-2026-09-08-004` (`penny-pincher` persona wording narrowed). README install section now leads with the GUI installer (`CHG-2026-09-08-012`). **`ERR-2026-09-06-001` deliberately untouched.** Verification: `test_nf_tier_enforcement` 30, `test_pbf_canon_loader` 6 (both re-run green on the rebased tree), `test_config` 134 (3 pre-existing `test_apply_profile_override` Windows-lane fails, proven on clean tree), installer `cargo test` 17 + `tsc`/`eslint` clean, `secret-guard.sh` 31 (`sh`+`dash`), `content-scan` `run.sh` 13; `scripts\collect-logs.ps1` self-check → **COMPLETE, 0 FAIL** — the sole WARN is 9 uncommitted files from a *separate concurrent pass* (`CONTRIBUTING.md`, `cron/jobs.py`, `hermes_time.py`, `hermes_cli/{voice,auth_model_picker}.py`, + tests), deliberately not touched or committed here. Pushed to `origin/main` `8409e486d8..f5ef1e6ac4` (rebased clean over a +33-commit `origin/main` advance). Prior: `RUN-2026-09-08-003` — **NF-v0.7.0 reconciled, committed per-CHG, rebased clean onto `origin/main`, and PUSHED** (`0333d48214..b22d5bc33d`, 9 commits = 2 `NF-v0.6.1` ledger + `CHG-2026-09-08-001..007`). `origin/main` had moved `+16 → +237` (a `NousResearch:main` merge) between analysis and push; re-verified none of the 237 touch any `CHG-001..007` file, rebase 0-conflict, `test_config` 134 + `test_bootstrap_launcher_hardening` 13 + `test_nf_tier_enforcement`/`test_profile_distribution` 123 all green on the rebased tree, `test_profiles.py` ×4 fails proven pre-existing on clean `origin/main`. Batch 1 (`CHG-2026-09-08-007`) landed as its own commit after fixing a PS-5.1 test-harness bug (`& $scriptblockParameter` drops `$LASTEXITCODE`; `Invoke-Native` code was correct). Editions (`CHG-2026-09-08-006`): `penny-pincher` (fresh) + `pine-barron-farms` (persona/method/guardrails only — canon packet + personal detail deliberately NOT in the public repo; deploy-time admin-gated) + `field-service` manifest backfill; `nf-setup.ps1` auto-installs `editions/<pin>` as a Hermes profile distribution before writing the signed record; verified by 4 `nf-setup.ps1 -NonInteractive` provisioning runs. Task 3: native image-gen needs no build (`image_generate` in core toolset; `plugins/image_gen/openai`+`xai`+…), enable = provider key; video/TTS/vision/free-web-search/browser also native key-only; music-gen absent. `git fetch upstream` 429'd (rate-limit) — coordinate taken from `origin/main` (contains `upstream/main`, 0 behind). Untracked `logs (2).zip` at repo root = junk, left alone. *Reconcile (no git mutations, 2 owner decisions pending):* ground truth ≠ the ledger's old claim — NF-v0.7.0 is **uncommitted** (working-tree/untracked on `85518051ed` = NF-v0.6.1 base); `origin/main` = same base +16 (11 upstream, contains `upstream/main` ⇒ 0 behind; +5 owner merge/fmt); none touch `logs/ledger/` content ⇒ rebasing the 2 ledger commits is conflict-free. `520e63661c`/`c111ede3e5` do **not** touch `config.py`/`config_home.py` (they're `config_providers`/`model_switch_providers`/`models`/`command_token_source`) ⇒ no textual or semantic conflict with `CHG-2026-09-08-002`'s `_ensure_default_skin()`; `test_config.py` 134 pass. The 3 "stray" files = **Codex Audit Batch 1** (`RUN-2026-09-07-010`'s "hold for review" batch): finished; `13/13` after this run fixed a test-harness bug (`& $scriptblockParameter` drops `$LASTEXITCODE` on PS 5.1 — `Invoke-Native` itself was always correct). Open: (i) commit/push scope for NF-v0.7.0, (ii) commit Batch 1 now vs hold. *Editions (`CHG-2026-09-08-006`):* new `editions/penny-pincher/` (fresh budgeting persona) + `editions/pine-barron-farms/` (persona/method/guardrails migrated from `D:\KB_PROJECT_2026\PBF_v22_FINAL`; **canon packet + personal detail deliberately not migrated** — public repo; deploy-time admin-gated per `canon/README.md`); `field-service/` backfilled `distribution.yaml`. Each edition is a Hermes profile distribution; `nf-setup.ps1` gains an auto `hermes profile install editions/<pin>` step (+`-SkipEditionInstall`, comma-list fix, `Invoke-Hermes`). Verified by throwaway `nf-setup.ps1 -NonInteractive` provisioning runs (Basic×3 + Full×1): edition installs to `profiles/<name>/`, record signed, `nf_tier.enforce_startup_profile` — Basic locks to pin / blocks others, Full switchable. Task 3: image-gen is already native (`image_generate` in core toolset; `plugins/image_gen/openai`+`xai`+…), enable = provider key only; video/TTS/vision/free-web-search/browser also native key-only; music-gen absent. Prior: `RUN-2026-09-08-002` — **`docs/preview.html` rebuilt on the real brand art** (`CHG-2026-09-08-005`, supersedes `CHG-2026-09-08-004`). AGENT D cosmetic pass: the `-004` draft hand-drew an inline-SVG mark; the shipped assets are now the page — `assets/banner.png` as the hero, `assets/icons/splash-alt.png` in the skin-detail block, `assets/icons/icon-256.png` as favicon + footer mark, each down-scaled and embedded as a `data:` URI (WebP for the two line-art panels, PNG for the icon; ~89 KB base64 total). Still one file, opens by double-click, **zero** network loads (four `href` links only), no JS. Palette + named swatches lifted verbatim from `skins/north-forge.yaml`; CSS session mock refreshed to `NF-v0.7.0` / `upstream 2237be35` + `│` tool line; footer carries both `LICENSE` copyright lines. Verified by a headless-Chrome full-page render (hero / what-it-is / themed-session mock / engine grid / footer) + HTML parse; not wired into anything. Prior: `RUN-2026-09-08-001` — **four queued passes, one session; committed to local `main`, not pushed** (`NF-v0.7.0`, `CHG-2026-09-08-001..004`). (1) **North Forge Setup GUI** — `apps/bootstrap-installer/` retargeted per `logs/CODEX-INSTALLER-GUI-FEASIBILITY-2026-09-07.md` §3: rebrand chrome + copy + mark; strip the `-Manifest`/`-Stage` protocol, `update.rs` (+`--update`), `install_script.rs` (GitHub download/cache/pin) + `build.rs` pin-baking, the Electron desktop-launch path, the macOS fast-path, 6 now-dead crates; new `src/repo.rs` (checkout resolution env→exe→drive-scan, sibling `-venv`/`-data` derivation, drive enum) + `src/routes/location.tsx` (drive/folder picker, rejects system drive); `bootstrap.rs` now shells `bootstrap-north-forge.ps1 -RepoRoot <root>` **once** into a single indeterminate progress + the kept log panel; Success "Launch" runs `north-forge.cmd` / `Start North Forge.lnk`. Toolchain unblocked first: npm→11.19.1 (user global; repo `engine-strict` bars 11.10–11.16), Windows 11 SDK 10.0.26100 (BuildTools 2019 had cl.exe, no SDK). `cargo test` 9/9; `tsc`+`eslint`+`vite build` clean; `tauri build` → `North-Forge-Setup.exe` (6.8 MB, branded version resource). The exact PS command the exe issues verified **fresh** (venv rebuild + `uv pip install -e .` → "READY") and **already-bootstrapped** against `D:\north-forge-agent`; `north-forge.cmd --version` → preflight PASS×4 → `hermes` runs. **`bootstrap-north-forge.ps1` / `nf-preflight.ps1` NOT touched.** Standing item confirmed: the frontend still `@import`s `apps/desktop/src/styles.css` wholesale and `@nous-research/ui@0.18.2` stays a real dep (the `Collapse-Bold` wordmark font loads from its `dist/`) — not fixed this pass (out of scope). (2) **Enforced first-launch skin** — `config._ensure_default_skin(home)` at `initialize_home` + `nf-setup.ps1` provisioning step: fresh fork-seeded `HERMES_HOME` now loads `north-forge`, not stock `default`; explicit user skin never overridden; managed skipped; `+7` tests. (3) **Docs sweep** — `logs/ledger/README.md` version line de-hardcoded; `README.md` "Drive class" made accurate (`nf-setup.ps1 -Show`); everything else (pyproject `0.21.1` = engine ver, CHANGELOG↔INDEX agreement, translated READMEs, `editions/**`, `.githooks/README.md`, INDEX open/resolved tables) verified consistent. (4) `docs/preview.html` single-file tease page. **Not pushed:** `origin/main` is +11 upstream ahead of local; a `git pull --rebase` re-integrating those 11 (incl. `520e63661c`/`c111ede3e5` model-discovery in `config`/`setup` — re-verify `CHG-2026-09-08-002` after) is an owner call. A separate pass's in-flight edits (`north-forge.cmd`, `scripts/bootstrap-north-forge.ps1`, `tests/test_bootstrap_launcher_hardening.py`) left untouched/uncommitted. Pre-existing unrelated failure noted: `tests/hermes_cli/test_skin_cmd.py::test_set_forks_a_builtin_without_inventing_a_background` (cp1252 `read_text()` in the test — fails on clean `main` too). Prior: `RUN-2026-09-07-011` — **ledger ratification pass (ledger-only, not pushed)**. `DECISION-2026-09-06-003` (install model — drive-native run-in-place vs machine-local managed install) ratified **DECIDED — A (drive-native run-in-place)**; `DECISION-2026-09-06-002` (attic clone) closed **DECIDED — A (delete)** — same batch, owner call. Rationale for -003: the sibling-venv layout + launch-readiness probe (`scripts/nf-preflight.ps1` + `scripts/lib/nf-readiness.ps1`) + tier/pin (`hermes_cli/nf_tier.py`, on-drive `provisioning.json`/`.nf-key`) already are a working, tested implementation of exactly that model (no host state, portable by design), across `NF-v0.5.1`→`NF-v0.6.0` on the original minimal bootstrap `CHG-2026-09-07-005` — load-bearing: `CHG-2026-09-07-015` (path-safety guard), `-020` (readiness probe), `-022` (tier/pin). Independently confirmed by two Codex audits (`logs/CODEX-AUDIT-2026-09-07.md`; `logs/CODEX-HERMES-EDITION-REUSE-CHECK-2026-09-07.md`). B (machine-local) / C (hybrid) rejected. The parked **hardened form** (drive seal / dual-volume `NORTHFORGE`+`NORTHFORGE-DATA` split / certify-verify-audit) is now **unblocked but not mandated** — a separate scheduling call; still the ratified home for `DECISION-2026-09-07-003`'s deferred non-drive-resident key store. -002: `origin/main` carries all work, zero unique commits in the ~869 MB copy — `D:\north-forge-agent-attic\nested-clone-2026-09-06\` **deleted this run** (~895 MB reclaimed, after verifying `820106d4a5` is an ancestor of `origin/main` with no local branches / stashes / uncommitted work); sibling `marguerite-and-penny-suno.txt` untouched. `CHG-2026-09-07-023` / `NF-v0.6.1` (PATCH — ledger-only per the version table). Working tree also held unrelated in-flight edits from a separate pass (`north-forge.cmd`, `scripts/bootstrap-north-forge.ps1`, `tests/test_bootstrap_launcher_hardening.py`) — **not** touched or committed here (only `logs/ledger/` was staged). Prior: `RUN-2026-09-07-010` — **tier / pinned-edition access control** (`CHG-2026-09-07-022` / `NF-v0.6.0`, `DECISION-2026-09-07-003` DECIDED). Queued research note `logs/CODEX-VISION-SKETCH-2026-09-07.md` was never produced → did the check inline (found: `config.yaml` `skills.disabled` is the only existing config-driven, dispatch-enforced control — `tools/skills_tool.py:553`; profiles have isolation but no lock; no "tier" concept anywhere). Chose **edition = Hermes profile**; tier/pin = a signed `provisioning.json` + `hermes_cli/nf_tier.py` + gates at all six profile-selection paths (`_apply_profile_override`, `resolve_profile_env`, `set_active_profile`, `profile_cmd`, dashboard router, `/edition`). Basic-tier: explicit `-p <non-pin>` or a foreign `HERMES_HOME` **exits non-zero**, `HERMES_HOME` never moves; stale `active_profile` silently ignored → pin; a tampered record refuses to start (absent record = inert). `scripts/nf-setup.ps1` new (Setup Run; not run by bootstrap). Tests: `tests/test_nf_tier_enforcement.py` **19 passed** (unit + real-subprocess integration + Windows-only `nf-setup.ps1`); NF launcher suites **21 passed**; `test_commands*`/`test_config` **208 passed**; `ruff` clean; pre-existing Windows-lane failures in `test_apply_profile_override.py`/`test_profiles.py` unchanged vs clean `main`. Signature is **tamper-evident, not tamper-proof** — recorded honestly in the DECISION; hardened key store deferred to `DECISION-2026-09-06-003`. **Next:** Codex Audit Batch 1 (bootstrap PS-5.1 stderr, checkout-rename data-dir, data-dir creation check) — separate commit, hold for review. Prior: `RUN-2026-09-07-009` — **CLI skin full re-theme (version 1 of the rebuild)**: `CHG-2026-09-07-021` / `NF-v0.5.5`. Found the built-in `charizard` skin's density technique (static Braille-pattern silhouette, U+2800–U+28FF, per-line Rich gradient) and reused it: `skins/north-forge.yaml` now ships a 30-key forge `colors:` block (dark-iron status bar, ember response border, themed completion/clarify/approval), a full `spinner:` block (forge verbs), `tool_prefix "│"`, and a Braille compass-rose + anvil + flame `banner_hero` (30×16, generated offline from `assets/icons/icon-512.png` + a redrawn bold compass — blueprint line-art was below Braille's resolution floor). Palette passes the built-in WCAG audit (STRONG ≥ 3.9 / SOFT ≥ 2.8). 73 skin+launcher tests pass; real launcher starts clean. Rendered before/after preview + a numbered refinement list in `D:\logs\SKIN-REBUILD-V2_2026-09-07.md`. Prior: `RUN-2026-09-07-008` — **critical launcher fix, landed alone**: `CHG-2026-09-07-020` / `ERR-2026-09-07-006` (Codex `F-05`, reclassified CRITICAL after a real first-handoff failure). The launcher no longer treats "`hermes.exe` exists" as environment readiness — a venv is not portable between machines. New `scripts/lib/nf-readiness.ps1` = a four-check launch-time probe (python executes / `import hermes_cli` / resolved module is under **this** checkout / `.nf-bootstrapped` `repo=` matches this checkout). New `scripts/nf-preflight.ps1` (run by `north-forge.cmd` before every launch): all pass → start; any fail → **silently rebuild the venv, never the data folder**, re-probe. One line per launch appended to `<parent>\<checkout>-launcher.log` *before* Python is invoked (timestamp, host, drive/repo, per-check PASS/FAIL, action, result). `bootstrap-north-forge.ps1` early-return now probe-gated. `tests/test_nf_preflight_readiness.py` 10 passed; `test_bootstrap_north_forge_path_safety.py` still 11 passed; real `.\north-forge.cmd --version` probes green then starts. `NF-v0.5.4` cut + pushed. **Next queued:** the docs-only batch (`DOC-01..DOC-10`, `logs/CODEX-DOCS-AUDIT-2026-09-07.md`); `R-03` steps 4–6 (marker Python-ABI/arch fields, native-Windows path/UNC test matrix, data-side diagnostic log). Prior: `RUN-2026-09-07-007` (`F-03` content-scan bypass, `CHG-2026-09-07-019`, `NF-v0.5.3`); `RUN-2026-09-07-006` (Codex cheap fixes + back-log, `NF-v0.5.2`); `RUN-2026-09-07-005` (F-04 data-loss fix, `NF-v0.5.1`); `RUN-2026-09-07-004` (drive-native onboarding, `NF-v0.5.0`); `RUN-2026-09-07-003` (push/sync). | 2026-09-10 |
| First fork commits | `NF-v0.1.0` + `NF-v0.1.1` on `origin/main` (`CHG-2026-09-06-010`); fork synced to upstream (`CHG-2026-09-06-014`) | 2026-09-06 |
| Fork identity | **full rebrand** — `NF-v0.2.0` (`CHG-2026-09-06-020..024`); branding-pass finish `NF-v0.2.1`; `NF-v0.3.0` completed the branding (final banner + `assets/icons/`, generic `SOUL.md` + `editions/field-service/` overlay, `BRANDING.md` source of truth, translated-README landing pages). `DECISION-2026-09-06-001` DECIDED. Pushed. | 2026-09-07 |
| Security guards | `.githooks/secret-guard` (filenames) + `.githooks/content-scan` (AWS/GitHub/Slack content, `--commits` gate + CI) + `scripts/redact_handoff.py` (mandatory handoff redaction, fail-closed). `CHG-2026-09-07-001/002`. | 2026-09-07 |

## Audits

| Audit | Date | Version | Scope | Open findings |
| --- | --- | --- | --- | --- |
| [AUDIT-2026-09-06-001](./audits/AUDIT-2026-09-06-001-repository-baseline.md) | 2026-09-06 | `NF-v0.1.0` / `hermes@820106d4a5` | Baseline: provenance, divergence, hygiene, secrets, CI, full README review. F-01–F-10 (F-09/F-10 added same session). | `ERR-2026-09-06-001` (HIGH), `ERR-2026-09-06-002` (MEDIUM) |
| [AUDIT-2026-09-06-002](./audits/AUDIT-2026-09-06-002-hardening-commit-and-hygiene.md) | 2026-09-06 | `NF-v0.1.1` / `hermes@820106d4a5` | Commit the baseline hardening set to `main`; clear recurred Windows/pytest cruft; git-freshness check on all 3 `D:\` repos; `.git` gc. F-01–F-07. | `ERR-2026-09-06-001` (HIGH), `ERR-2026-09-06-002` (MEDIUM) — both carried, none new open |

## Open incidents (faults)

| ID | Sev | Summary |
| --- | --- | --- |
| [ERR-2026-09-06-001](./errors/ERROR-LOG.md#err-2026-09-06-001--high--secret-hygiene) | HIGH | Live `ANTHROPIC_API_KEY` in `.env` (and `D:\.env`). Exposure mitigated **and committed** (`.githooks/` + `.gitignore`); confirm/rotate still pending. **Accepted, time-limited, owner-decided — left as-is on purpose.** |
| [ERR-2026-09-08-012](./errors/ERROR-LOG.md#err-2026-09-08-012--low--nf-setupps1-reports-provisioning-failed-exit--when-lastexitcode-is-null) | LOW | `scripts/nf-setup.ps1`: `$script:NfExit = $LASTEXITCODE` can be `$null` if `& $pyExe` never launches → `Provisioning failed (exit )`. **Fails safe** (no record written); message uninformative. Access-tier code — coerce null→nonzero in a dedicated change. |
| [ERR-2026-09-10-001](./errors/ERROR-LOG.md#err-2026-09-10-001--high--import-hermes_climain-raises-nameerror-_desktop_ssh_backend-undefined) | HIGH | A merge dropped `def _desktop_ssh_backend` from `hermes_cli/main.py` but kept its call in `_apply_profile_override` (`main.py:642`), and the module-level call at `main.py:679` makes `import hermes_cli.main` raise `NameError` on an unprovisioned / Full-tier drive. On `origin/main` too. **Not caused by `CHG-2026-09-10-001`** (reproduced on the reverted tree); not self-fixed — access-tier code + out of task scope. 1-line verbatim restore from `677e8ed8a4`. |

_(`ERR-2026-09-08-005..011` opened + resolved `RUN-2026-09-08-005`; `ERR-2026-09-07-004` / `-007` closed `RUN-2026-09-08-004` — see Resolved incidents.)_

## Open decisions (judgment calls)

_None._ (`DECISION-2026-09-07-002` closed `RUN-2026-09-08-004`; see Resolved decisions.)

## Resolved decisions

| ID | Area | Question | Outcome |
| --- | --- | --- | --- |
| [DECISION-2026-09-11-001](./decisions/DECISION-LOG.md#decision-2026-09-11-001--repo-safety--how-to-stop-github-fork-sync-from-silently-discarding-fork-history-a-third-time) | Repo safety | How to stop GitHub fork-sync from silently discarding fork history a third time? | **DECIDED** — C+B: GitHub branch protection on `main` (`allow_force_pushes:false`, `allow_fork_syncing:false`, `enforce_admins:true` — the structural fix) plus `nf-branding-guard.yml` CI content check as detection. A (exclude branding files from merge conflict resolution) not pursued — the fault was a branch-level reset, not a merge conflict. `RUN-2026-09-11-002` / `CHG-2026-09-11-003`+`-004`. Resolves ERR-2026-09-11-001. |
| [DECISION-2026-09-10-001](./decisions/DECISION-LOG.md#decision-2026-09-10-001--access-architecture--how-does-a-full-tier-operator-re-open-setup-run-from-inside-a-running-session) | Access architecture | How does a Full-tier operator re-open Setup Run from inside a running session? | **DECIDED** — A (in-session admin-passcode trigger → `nf-setup.ps1 -Force` after teardown, then re-exec; Full-tier + passcode-set + exact-match only, silent + inert on every other input, recognised attempts logged for the owner without the passcode), `RUN-2026-09-10-001` / `CHG-2026-09-10-001` (`NF-v0.10.0`). Owner-directed. Grants nothing a Full operator lacks; no enforcement path changed. Builds on `DECISION-2026-09-07-003`. |
| [DECISION-2026-09-09-001](./decisions/DECISION-LOG.md#decision-2026-09-09-001--drive-provisioning--bundle-a-python-toolchain-on-the-drive-and-how) | Drive provisioning | Bundle a Python toolchain on the drive, and how? | **DECIDED** — B (bundle `uv.exe` + a `python-build-standalone` CPython 3.11 as a never-git-tracked drive sibling `<parent>\<leaf>-toolchain\`, admin-prepared once and copied per drive — not fetched at first-launch, not git/LFS), `RUN-2026-09-09-002` / `CHG-2026-09-09-003` (`NF-v0.9.0`). `bootstrap` resolves bundled → host PATH (admin/dev, logged) → the existing error. Completes `DECISION-2026-09-06-003`; no change to R1. |
| [DECISION-2026-09-07-002](./decisions/DECISION-LOG.md#decision-2026-09-07-002--rebrand-claim-wording--engine-used-unmodified--full-rebrand) | Branding wording | "engine used unmodified" / "full rebrand" broader than the code (Codex `F-07`)? | **DECIDED** — A (tighten wording), `RUN-2026-09-08-004` / `CHG-2026-09-08-010`+`-011`. README ×2 + 3 translations + BRANDING ×2 reworded to *"keeps Hermes Agent's functional engine behavior; downstream changes are identity, presentation, and workflow only"*; `cli.py` welcome + `_parser.py` `chat` description moved to North Forge; BRANDING now names the deliberately-Hermes visible surfaces. No identifier churn. |
| [DECISION-2026-09-06-003](./decisions/DECISION-LOG.md#decision-2026-09-06-003--install-model--drive-native-run-in-place-vs-machine-local-managed-install) | Install model | Drive-native run-in-place vs machine-local managed install? | **DECIDED** — A (drive-native run-in-place), `RUN-2026-09-07-011` / `CHG-2026-09-07-023` / `NF-v0.6.1`. The sibling-venv layout + launch-readiness probe (`nf-preflight.ps1`) + tier/pin (`nf_tier.py`, `NF-v0.6.0`) already implement it — verified in-tree and by two Codex audits. Implementing range `NF-v0.5.1`→`NF-v0.6.0` on `CHG-2026-09-07-005`. B (machine-local) / C (hybrid) rejected. Hardened form (drive seal / dual-volume / certify) now **unblocked, not mandated** — separate scheduling call; still the home for `DECISION-2026-09-07-003`'s deferred key store. |
| [DECISION-2026-09-06-002](./decisions/DECISION-LOG.md#decision-2026-09-06-002--attic-clone--keep-or-delete-it) | Repo hygiene | Keep or delete the attic clone (~869 MB)? | **DECIDED** — A (delete), `RUN-2026-09-07-011` / `CHG-2026-09-07-023` / `NF-v0.6.1`. `origin/main` carries all work, zero unique commits in `D:\north-forge-agent-attic\nested-clone-2026-09-06\`, fully reconstructible by `git clone`. **Deleted 2026-09-07** (~895 MB reclaimed; verified `820106d4a5` is an ancestor of `origin/main`, no local branches/stashes/uncommitted work first); sibling `marguerite-and-penny-suno.txt` untouched. |
| [DECISION-2026-09-07-003](./decisions/DECISION-LOG.md#decision-2026-09-07-003--edition--tier-mechanism--what-is-an-edition-and-how-is-basic-tier-enforced) | Access architecture | What is an "edition", and how is Basic tier enforced? | **DECIDED** — A (edition = Hermes profile; HMAC-signed `provisioning.json` + gates at every profile-selection path), `RUN-2026-09-07-010` / `CHG-2026-09-07-022` / `NF-v0.6.0`. Signature is tamper-evident not tamper-proof (stated in the entry); hardened on-drive key store deferred to `DECISION-2026-09-06-003`. |
| [DECISION-2026-09-07-001](./decisions/DECISION-LOG.md#decision-2026-09-07-001--splash-art--keep-the-stock-hermes-launch-mark-or-swap-it) | Branding | Keep the stock Hermes launch splash, or swap it? | **DECIDED** — C (swap via a North Forge skin), `RUN-2026-09-07-004` / `CHG-2026-09-07-012`. `skins/north-forge.yaml` carries `banner_logo`/`banner_hero`; `hermes_cli/banner.py` unchanged. |
| [DECISION-2026-09-06-001](./decisions/DECISION-LOG.md#decision-2026-09-06-001--fork-identity--rebrand-vs-thin-downstream) | Fork identity | Rebrand vs thin downstream? | **DECIDED** — B (full rebrand), implemented `RUN-2026-09-06-002` / `NF-v0.2.0` (`CHG-2026-09-06-020..024`). `pyproject.toml` distribution name kept as `hermes-agent` per the entry's carve-out. |

## Resolved incidents

| ID | Sev | Summary | Fixed by |
| --- | --- | --- | --- |
| ERR-2026-09-06-002 | MEDIUM | Fault half (fork 2 behind upstream) fixed; choice half → `DECISION-2026-09-06-001` | CHG-2026-09-06-014 / migrated |
| ERR-2026-09-06-003 | MEDIUM | `.gitignore` missed `.env.production` / `.env.<name>` | CHG-2026-09-06-007 |
| ERR-2026-09-06-004 | LOW | Stray `%SystemDrive%` Windows cache tree in repo root (recurred; guard held) | CHG-2026-09-06-009 / -012 |
| ERR-2026-09-06-005 | LOW | pytest/mock artifacts (`MagicMock/`, `C:Users…`, `logs.zip`) in working tree | CHG-2026-09-06-011 / -012 |
| ERR-2026-09-07-001 | LOW | `bootstrap-north-forge.ps1` uv cache on `C:` vs venv on checkout drive → cross-volume full-copy, ~6.5 min first run | CHG-2026-09-07-008 |
| ERR-2026-09-07-002 | LOW | Upstream test files call `os.geteuid()` in an eager `skipif` decorator arg → unfiltered `pytest` aborts at collection on Windows. Pre-existing upstream; NF touches none of the files; targeted runs green | **ACCEPTED-RISK** — CHG-2026-09-07-014 (owner: no shim; Linux CI + targeted runs; revisit if upstream fixes or a full local Windows run is needed) |
| ERR-2026-09-07-003 | HIGH | Codex F-04 (data-loss): `bootstrap-north-forge.ps1` path guard missed venv/data *equal to* the checkout → `-VenvDir <repo>` + `-Force` = `Remove-Item -Recurse` on the tree. Default `north-forge.cmd` path unaffected | CHG-2026-09-07-015 (canonicalize + reject equal/inside/contains, both dirs; + `tests/test_bootstrap_north_forge_path_safety.py`) |
| ERR-2026-09-07-005 | HIGH | Codex F-03: `.githooks/content-scan` `--commits` gate word-split space/tab/dash/Unicode paths → a planted `ghp_` token in `dir/file name.txt` passed pre-push **and** `nf-secret-scan.yml` CI | CHG-2026-09-07-019 (`--commits` reads `git diff-tree -z`, scans by post-image blob OID, never by path string; `.githooks/tests/run.sh` +7 cases; verified before/after) |
| ERR-2026-09-07-006 | **CRITICAL** (was MEDIUM) | Codex F-05, reclassified after a real first-handoff failure: `north-forge.cmd` + `bootstrap-north-forge.ps1` trusted "`hermes.exe` exists" as readiness; a venv is not portable between machines and nothing probed it before launch | CHG-2026-09-07-020 (four-check launch-time probe `scripts/lib/nf-readiness.ps1`; `scripts/nf-preflight.ps1` silent venv-only rebuild on any failure + one launcher-log line per launch; `bootstrap` early-return probe-gated; +`tests/test_nf_preflight_readiness.py` 10) |
| ERR-2026-09-07-004 | HIGH | Codex F-02: `collect-logs.{sh,ps1}` completeness check compared report/ledger **dates**, not `RUN-` id to report — one same-day report covered every run that day | CHG-2026-09-08-008 (`RUN-2026-09-08-004`) — `scripts/lib/report_completeness.py` + `logs/ledger/reports/REPORT-MANIFEST.md`: every ledger `RUN-` id must map to a report (or explicit ledger-only); missing/mismatched ⇒ **FAIL**. `SESSION-REPORT-TEMPLATE.md`; 4 un-reported runs backfilled |
| ERR-2026-09-07-007 | MEDIUM | Codex F-09: filename block was `.env`-family only (`credentials.json` / `id_rsa` / `*.pfx` / `*.p12` / SA-JSON not blocked) | CHG-2026-09-08-009 (`RUN-2026-09-08-004`) — `secret-guard` broadened to SSH keys / `*.p12`·`*.pfx`·`*.jks`·`*.keystore` / `credentials.json`·SA-JSON / `.netrc`·`.pgpass`·`.htpasswd` / PEM·`.key` (public-bundle + `tests/` carve-outs, `ALLOWLIST` hatch); `+31` self-tests; CI runs both hook suites + `secret-guard --tree/--range`. Broad-provider content scanner still a future add, not an open fault |
| ERR-2026-09-08-001 | HIGH | Codex PC-2026-09-08-001: `nf_tier.load()` raised on a signed-but-malformed record → `_nf_tier_gate` `except: return None` → enforcement silently off | CHG-2026-09-08-013 (`RUN-2026-09-08-004`) — `load()` made total (every signed field type/bounds-checked, all failures ⇒ `STATE_TAMPERED`); `_nf_tier_gate` fails closed when a record file is present; `+11` tests (exact repro as unit + integration) |
| ERR-2026-09-08-002 | MEDIUM | Codex PC-2026-09-08-002/003: `apps/bootstrap-installer` trusted the frontend `repoRoot` (no re-check it's a real checkout / not the system drive); multi-drive autodetect took the first match | CHG-2026-09-08-014 (`RUN-2026-09-08-004`) — one shared `repo::validate_target` gate for the picker **and** `run_bootstrap` pre-spawn; autodetect only when the global checkout count is exactly 1, else force a pick + disable Install. `+8` Rust tests |
| ERR-2026-09-08-003 | HIGH | Codex PC-2026-09-08-004: `editions/pine-barron-farms/SOUL.md` claims "I follow the loaded canon packet" but nothing loaded `canon/PINE_BARRON_FARMS_CANON.md` | CHG-2026-09-08-015 (`RUN-2026-09-08-004`) — `canon/load_canon.py` (receipt: path + sha256 + bytes, or a not-found warning) loaded via a SOUL-mandated session-start command + the new `skills/pbf-canon/` skill; no engine change; `tests/test_pbf_canon_loader.py` (6, incl. assembled-context proof) |
| ERR-2026-09-08-004 | MEDIUM | Codex PC-2026-09-08-005: `editions/penny-pincher/SOUL.md` oversells ("I hold the running picture", "tracking bills") — no ledger mechanism; affordability answers didn't name what they omit | CHG-2026-09-08-016 (`RUN-2026-09-08-004`) — wording only: "I write it down", "no bank connection / no ledger of its own", affordability answers state what's missing. README matched |
| ERR-2026-09-08-005 | MEDIUM | `hermes_logging._add_rotating_handler()` check-then-append race: dedup scan outside `_queue_state_lock`, append inside → two `setup_logging()` threads each register a handler for one file | CHG-2026-09-08-017 (`RUN-2026-09-08-005`) — `_handler_covers_path()` extracted; pre-check under the lock, `_register_queued_handler(dedup_path=…)` re-checks under the lock (decision+append one critical section), loser closed after release. +1 concurrency test. Corrected per independent review |
| ERR-2026-09-08-006 | MEDIUM | `tests/test_nf_tier_enforcement.py` gated its `nf-setup.ps1` drive with a bare `_WINDOWS_ONLY = skipif(...)`; `list_os_marked_tests.py` matches `windows_only` whole-word → the CI Windows lane never imported the file → the test never ran in CI | CHG-2026-09-08-018 (`RUN-2026-09-08-005`) — `@pytest.mark.windows_only` (conftest still skips off-win32); file now in the lane list, executed green on this win32 host |
| ERR-2026-09-08-007 | MEDIUM | `worktree_gc._archive_untracked()` writes reclaimed untracked scratch to a hardcoded `~/.hermes/archive/worktree-prune`, ignoring `HERMES_HOME` / profile / managed home | CHG-2026-09-08-019 (`RUN-2026-09-08-005`) — `dest = get_hermes_home()/"archive"/"worktree-prune"/…`; fail-safe unchanged; fixture splits HOME/HERMES_HOME; +1 archive-failure test |
| ERR-2026-09-08-008 | MEDIUM | `hermes logs`: (A) filtered path over-reads only `max(N*20,2000)` tail lines → a match behind a wall of non-matches is missed; (B) per-line filter leaks a rejected record's continuation lines / drops a matched record's | CHG-2026-09-08-021 (`RUN-2026-09-08-005`) — `_iter_records()` groups header+continuations; filter on the header only; filtered `_read_tail` streams the whole file into `deque(maxlen=N)`; `_FollowFilter` for `-f`. +12 tests |
| ERR-2026-09-08-009 | LOW | `cron.load_jobs()` guard `if jobs and repair:` — an empty legacy map repairs in memory but is never rewritten, so every load re-runs the repair path | CHG-2026-09-08-022 (`RUN-2026-09-08-005`) — `if repair:`; file rewritten to `{"jobs": []}`, next load idempotent. Test extended |
| ERR-2026-09-08-010 | MEDIUM | `auth_model_picker._confirm_selection_guards()` `except Exception: warnings = []` — a guard-registry error is indistinguishable from "no warnings", so a flagged model is saved silently | CHG-2026-09-08-023 (`RUN-2026-09-08-005`) — non-`ImportError` → log + `False` (model unchanged); `ImportError` still `True`. +4 tests |
| ERR-2026-09-08-011 | MEDIUM | `hermes_time.get_timezone()` publishes `(name, tz)` under a cache identity captured before the outside-lock resolve; a mid-resolve profile / `HERMES_TIMEZONE` switch poisons the slot or returns the wrong profile's zone | CHG-2026-09-08-025 (`RUN-2026-09-08-005`) — re-read the identity after the resolve, retry on a shift. +2 tests |
| ERR-2026-09-09-001 | HIGH | `bootstrap-north-forge.ps1` trusted a bare `hermes.exe`+marker check and `-Force`-deleted any existing `$VenvDir` — a partial/unrelated dir at `<checkout>-venv` was reused or destroyed with no ownership proof | CHG-2026-09-09-001 (`RUN-2026-09-09-001`) — readiness vs ownership split (new read-only `scripts/lib/nf-venv-state.ps1`), `create`/`rebuild`/`refuse`/`none` state table, `-Force` ≠ deletion override, re-check before the one `Remove-Item`. `+13` tests |
| ERR-2026-09-09-002 | HIGH | `bootstrap-north-forge.ps1` never checked `-VenvDir` and `-DataDir` against each other → an overlapping pair let a `-Force` rebuild delete `HERMES_HOME` (explicit-override path only) | CHG-2026-09-09-001 (`RUN-2026-09-09-001`) — new `Test-PathOverlap $VenvDir $DataDir` guard; equal/trailing-sep/case/nested rejected. `×4` tests |
| ERR-2026-09-09-003 | MEDIUM | `query_session_listing` fixed `limit*4` window + after-the-fact Python filter hid an older *named* session behind newer unnamed rows (same shape as `ERR-2026-09-08-008`) | CHG-2026-09-09-002 (`RUN-2026-09-09-001`) — adaptive widening `limit*(4,8,16)` from row 0; `limit<=0` ⇒ `[]` before any query; filter factored to `_displayable()`. `+5` tests |
| ERR-2026-09-11-001 | **CRITICAL** | GitHub fork-sync ("Sync fork" discard / `merge-upstream`) force-reset `origin/main` to an upstream `NousResearch/hermes-agent` commit — 2nd occurrence — wiping all 186 fork-only commits (`README.md` branding, `SOUL.md`, `BRANDING.md`, the ledger, CLI skin, installer). `main` had zero branch protection | CHG-2026-09-11-003 (branch protection incl. `allow_fork_syncing:false`) / CHG-2026-09-11-004 (`nf-branding-guard.yml`); recovery = force-push of local `main` (which retained full history); DECISION-2026-09-11-001 |
